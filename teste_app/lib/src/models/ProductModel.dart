class ProductModel {
  final int id;
  final String name;
  final double value;
  final String asset;
  final String description;

  ProductModel(this.id,this.name, this.value, this.asset, this.description);
}