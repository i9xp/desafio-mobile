import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:teste1/app/modules/favorites/favorites_controller.dart';
import 'package:teste1/app/modules/favorites/favorites_module.dart';

void main() {
  initModule(FavoritesModule());
  // FavoritesController favorites;
  //
  setUp(() {
    //     favorites = FavoritesModule.to.get<FavoritesController>();
  });

  group('FavoritesController Test', () {
    //   test("First Test", () {
    //     expect(favorites, isInstanceOf<FavoritesController>());
    //   });

    //   test("Set Value", () {
    //     expect(favorites.value, equals(0));
    //     favorites.increment();
    //     expect(favorites.value, equals(1));
    //   });
  });
}
