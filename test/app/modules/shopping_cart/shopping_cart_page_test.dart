import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:teste1/app/modules/shopping_cart/shopping_cart_page.dart';

void main() {
  testWidgets('ShoppingCartPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ShoppingCartPage(title: 'ShoppingCart')));
    //  final titleFinder = find.text('ShoppingCart');
    //  expect(titleFinder, findsOneWidget);
  });
}
