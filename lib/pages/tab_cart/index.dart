import 'package:flutter/material.dart';

class TabCart extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Content Cart"),
    );
  }

}