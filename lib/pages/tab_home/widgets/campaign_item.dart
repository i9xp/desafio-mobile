import 'package:flutter/material.dart';

class CampaignItem extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Campaign item widget"),
    );
  }

}